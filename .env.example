# Copy to .env.local and set a real value. Never commit .env.local.
# The content API (POST/PATCH/DELETE /api/articles) is disabled (503)
# until this is set. In production, pass it to the container as an env var.
CONTENT_API_TOKEN=change-me